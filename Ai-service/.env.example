# Origin Node serves /uploads from. Used to resolve relative floorPlanUrl
# paths like "/uploads/floor-plans/<userId>/<file>" into a fetchable URL.
# Change to Node's Docker service name (e.g. http://main-api:5000) once
# both services run in the same docker-compose network.
NODE_UPLOADS_BASE_URL=http://localhost:5000

MAX_IMAGE_FETCH_BYTES=20971520
REQUEST_TIMEOUT_SECONDS=15.0

MIN_WALL_LENGTH_PX=40
MIN_ROOM_AREA_PX=4000
